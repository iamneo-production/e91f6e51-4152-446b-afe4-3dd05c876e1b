import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: { main: "#0A4D68" },
    secondary: {
      main: "#1B9C85",
    },
  },
});

export default theme;
